# 实施计划

- [x] 根据用户反馈使用 GPT Image 2 生成第二版火刃仪表视觉稿，已保留旧稿作对比，并保存高清与 320x240 版本到 `preview/`。
- [x] 生成第三版视觉稿：恢复原图约 `65%` 圆弧结构、将单位移到中心上方，并将左侧列表精简为无边框/无图标的灰色标题行与白色数值行。
- [x] 生成第四版视觉稿：将圆弧起点移到 `6 点钟方向`，延长左侧标题条以紧贴新弧边，加深档位圆阴影，将 `GRIP ANGLE` 改为 `AVG. SPEED / 72 km/h`。
- [x] 生成第五版视觉稿：不只移动 `0`/指针，而是将整个约 `65%` 速度刻度环绕档位圆心整体逆时针旋转，使完整起始截面和 `0` 同时落在 `6 点钟方向`。
- [x] 生成第五版视觉稿：已验证整个约 `65%` 速度刻度环可共享单一旋转参数。
- [x] 生成第六版视觉稿：将整个刻度环相对 V5 旋转 `30°`，使物理起始截面、`0` 刻度和起点指针统一落在 `7 点钟方向`。
- [x] 以 V6 横屏稿为视觉基准生成竖屏效果图，已保存高清稿和 240x320 稿到 `preview/`，读图检查无重叠。
- [ ] 暂停并等待用户检查视觉稿；只有用户明确批准后才运行 `task.py start` 和后续代码步骤。
- [ ] 对将修改的现有函数/方法执行 GitNexus `impact --direction upstream`，报告直接调用者、受影响流程和风险级别；遇到 HIGH/CRITICAL 先向用户警告。
- [ ] 扩展 LVGL 公共合约：三态样式枚举、快照样式字段、新设置动作，保留现有 ABI 值顺序。
- [ ] 扩展 IDF runtime 的加载、校验、应用和保存路径，保留 NVS `0/1` 语义并接受 `2`。
- [ ] 在设置样式列表增加“本田火刃”，点击时发送精确样式值，选中态跟随快照。
- [ ] 新增单层火刃 LVGL 动态绘制，实现横/竖屏布局和无效数据状态，不修改用户当前未提交的旧仪表绘制调整。
- [ ] 模拟器快照默认为火刃，并处理三态设置动作。
- [ ] 执行格式化、模拟器编译、横/竖屏 headless 运行和截图，将验收图保存到 `preview/`并人工读图检查。
- [ ] 运行项目固件编译与 `trellis-check`；由于修改共享固件源码，按 `esp32-lan-rfc2217-flash` 流程烧录并监视启动日志。
- [ ] 提交前运行 GitNexus `detect_changes --scope compare --base-ref main`，确认只影响仪表样式、设置动作、持久化与模拟器流程。
