# 实施计划

1. 扩展 GSA/GSV 解析数据结构，提取 HDOP、单句 SNR 总和与有效 C/N0 数量；补充断言自测。
2. 在 GPS 状态和运行时快照中传递平均 SNR、HDOP；保持现有过期失效语义。
3. 用现有设置行组件替换 GPS 设置页中的基础状态字段，渲染测速质量诊断文案并扩展刷新比较。
4. 运行 GPS 解析自测、LVGL 模拟器 GPS 设置冒烟和正常 ESP-IDF 构建。

## Rollback

移除新快照字段与页面行即可恢复原有 GPS 设置页；不涉及持久化数据或硬件配置。
