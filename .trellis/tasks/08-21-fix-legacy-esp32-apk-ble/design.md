# 技术设计

Android 连接成功后先等待系统 Bond 状态，再发现服务；发现服务后订阅响应特征，若加密 CCCD 写入返回认证错误，短暂等待并重试一次，而不是立即把会话判为永久失败。所有失败路径统一清理 GATT、待处理请求和会话状态。

固件继续要求 API 请求写入和响应通知使用加密链路；`ENC_CHANGE` 成功后才标记本地蓝牙在线。这样保留配置读写的安全边界，同时兼容旧 ESP32/Android 的配对时序差异。
