# Core

- Workspace root: `/vol1/1000/项目/esp32BmsGps`.
- Product: ESP32 ESP-IDF firmware for GPS speed display, TFT touch UI, ANT BMS data display, BLE BMS binding, Setup AP, and local Web settings.
- Trellis is initialized in single-repo mode. Use `.trellis/tasks/` for task artifacts and `.trellis/spec/backend` / `.trellis/spec/frontend` for executable project conventions.
- Read `mem:tech_stack` before changing build/runtime tooling.
- Read `mem:conventions` before restructuring project boundaries.
- Read `mem:suggested_commands` and `mem:task_completion` for local validation, GitNexus, and completion commands.
- Read `mem:hardware/lan_rfc2217_flash_bridge` before flashing or monitoring hardware from this Linux host.
