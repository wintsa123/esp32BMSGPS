# Core

- Workspace root: `/vol1/1000/项目/esp32BmsGps`.
- The repository currently has no firmware source tree, build config, package manifest, README, or git history; only `AGENTS.md`, `.trellis/`, and `.serena/` are present.
- Trellis is initialized in single-repo mode, but its generated spec layers are generic `backend` and `frontend` placeholders from first-time init, not confirmed project conventions.
- Active product direction as of onboarding: ESP32 firmware for GPS speed display, TFT touch UI, Ant BMS data display, and local/Wi-Fi settings.
- Read `mem:tech_stack` before choosing or changing build tooling. Read `mem:conventions` before creating project structure. Read `mem:suggested_commands` and `mem:task_completion` for available local commands.